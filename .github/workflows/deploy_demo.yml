# This file was NOT auto-generated by the Firebase CLI

name: Deploy Demo
'on':
  push:
    branches:
      - master

env:
  REACT_APP_ENVIRONMENT: production
  REACT_APP_FIREBASE_API_KEY: ${{ secrets.FIREBASE_API_KEY_FAVOFLIX }}
  REACT_APP_FIREBASE_AUTH_DOMAIN: favoflix.firebaseapp.com
  REACT_APP_FIREBASE_PROJECT_ID: favoflix
  REACT_APP_FIREBASE_STORAGE_BUCKET: favoflix.appspot.com
  REACT_APP_FIREBASE_MESSAGING_SENDER_ID: 164550600575
  REACT_APP_FIREBASE_APP_ID: 1:164550600575:web:c8bc68bdadaffdd5419c4a
  REACT_APP_FIREBASE_USE_LOCALHOST_EMULATORS: false
  REACT_APP_ALGOLIA_SEARCH_API_KEY: ${{ secrets.ALGOLIA_SEARCH_API_KEY_FAVOFLIX }}
  REACT_APP_ALGOLIA_ID: ${{ secrets.ALGOLIA_ID_FAVOFLIX }}

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [ 14.x ]

    steps:
      - uses: actions/checkout@v2
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - run: npm install
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.MY_GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_FAVOFLIX }}'
          channelId: live
          projectId: favoflix
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
